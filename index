<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Palmetto Digital — professional websites and branding for service businesses." />
  <title>Palmetto Digital</title>
  <link rel="stylesheet" href="style.css" />

  <!-- Home-only enhancements (KingContractor-style: confident, clean, outcome-driven) -->
  <style>
    /* =========================
       HERO BANNER
       - Centered
       - Buttons stay side-by-side on mobile
       - More "established" look
       ========================= */
    .hero-banner{
      background:
        linear-gradient(180deg, rgba(11,30,58,.78), rgba(11,30,58,.78)),
        radial-gradient(900px 520px at 20% 10%, rgba(242,140,40,.18), transparent 60%),
        radial-gradient(820px 540px at 85% 15%, rgba(31,120,180,.16), transparent 55%),
        url("hero-bg.jpeg");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      min-height: calc(100vh - 76px);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hero-banner .container{
      display: flex;
      justify-content: center;
      width: 100%;
    }

    .hero-inner{
      text-align: center;
      color: #fff;
      width: 100%;
      max-width: 980px;
      margin: 0 auto;
      padding: 5rem 1rem;
    }

    /* Trust kicker (subtle, premium) */
    .hero-kicker{
      display: inline-flex;
      align-items: center;
      gap: .5rem;
      padding: .4rem .75rem;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,.20);
      background: rgba(255,255,255,.10);
      backdrop-filter: blur(8px);
      font-weight: 850;
      font-size: .92rem;
      letter-spacing: .2px;
      margin-bottom: .9rem;
    }

    .hero-inner h1{
      font-size: clamp(2.35rem, 4.2vw, 3.7rem);
      line-height: 1.04;
      margin: 0 0 .75rem;
      letter-spacing: -.7px;
      text-shadow: 0 12px 40px rgba(0,0,0,.45);
    }

    .hero-inner p{
      max-width: 72ch;
      margin: 0 auto 1.7rem;
      color: rgba(255,255,255,.90);
      text-shadow: 0 10px 28px rgba(0,0,0,.35);
      font-size: 1.05rem;
      line-height: 1.6;
    }

    /* Buttons centered + side-by-side on ALL sizes */
    .hero-actions{
      display: flex;
      justify-content: center;
      align-items: center;
      gap: .75rem;
      width: 100%;
      flex-wrap: nowrap;
      margin-top: .25rem;
    }

    .hero-actions .btn{
      white-space: nowrap;
      padding: .85rem 1.15rem;
    }

    /* Transparent orange primary CTA (matches your scheme) */
    .btn.hero-primary{
      background: rgba(242,140,40,.20);
      border: 1px solid rgba(242,140,40,.60);
      color: #fff;
      backdrop-filter: blur(8px);
    }

    .btn.hero-primary:hover{
      background: rgba(242,140,40,.30);
      box-shadow: 0 0 0 6px rgba(242,140,40,.16);
    }

    /* Secondary hero button slightly glassy */
    .btn.hero-secondary{
      background: rgba(255,255,255,.10);
      border: 1px solid rgba(255,255,255,.22);
      color: #fff;
      backdrop-filter: blur(8px);
    }

    .btn.hero-secondary:hover{
      background: rgba(255,255,255,.16);
      box-shadow: 0 0 0 6px rgba(31,120,180,.12);
    }

    /* Small trust line under buttons */
    .hero-trust{
      margin-top: 1.05rem;
      color: rgba(255,255,255,.82);
      font-weight: 750;
      font-size: .95rem;
    }

    .hero-trust span{
      display: inline-flex;
      align-items: center;
      gap: .45rem;
      padding: .38rem .65rem;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,.18);
      background: rgba(255,255,255,.08);
      backdrop-filter: blur(8px);
      margin: .3rem .25rem 0;
    }

    /* Mobile tighten */
    @media (max-width: 768px){
      .hero-banner{ min-height: calc(92vh - 76px); }
      .hero-inner{ padding: 4.2rem 1rem; }
      .hero-actions{ gap: .6rem; }
      .hero-actions .btn{
        width: auto;
        padding: .78rem .95rem;
        font-size: .95rem;
      }
    }
    @media (max-width: 380px){
      .hero-actions .btn{
        padding: .7rem .85rem;
        font-size: .9rem;
      }
    }

    /* =========================
       SERVICES – cleaner, more "agency" / contractor-style
       - fewer words
       - stronger hierarchy
       - glow on hover
       ========================= */
    .services-glow .grid{ align-items: stretch; }

    .services-glow .card{
      background: rgba(255,255,255,.84);
      border: 1px solid rgba(11,30,58,.10);
      box-shadow:
        0 10px 28px rgba(11,30,58,.10),
        0 0 0 0 rgba(31,120,180,0);
      transition: transform .2s ease, box-shadow .2s ease;
    }

    .services-glow .card:hover{
      transform: translateY(-3px);
      box-shadow:
        0 16px 40px rgba(11,30,58,.16),
        0 0 0 6px rgba(31,120,180,.10);
    }

    .services-glow .card h3{
      margin: .15rem 0 .35rem;
      letter-spacing: -0.2px;
    }

    .services-glow .card p{
      margin: 0;
      color: rgba(11,18,32,.74);
      line-height: 1.55;
    }

    /* Icon sizing polish */
    .icon.screen{ font-size: 1.4rem; }

    /* Mobile flow for services */
    @media (max-width: 720px){
      .services-glow .grid{ gap: .85rem; }
      .services-glow .card{ grid-column: span 12; padding: 1.05rem; }
      .services-glow .icon{
        width: 46px; height: 46px;
        border-radius: 14px;
        margin-bottom: .6rem;
      }
    }

    /* =========================
       FOOTER — centered
       ========================= */
    .footer .foot{
      justify-content: center !important;
      text-align: center;
    }
    .footer .foot > div{ width: 100%; }
  </style>
</head>

<body>

<header class="header">
  <a class="skip-link" href="#main">Skip to content</a>
  <div class="container">
    <div class="nav">
      <a class="brand" href="index.html" aria-label="Palmetto Digital home">
        <img src="palmetto-digital-logo.png" alt="Palmetto Digital logo" />
      </a>

      <button class="mobile-toggle" data-mobile-toggle aria-expanded="false" aria-controls="mobileMenu">
        ☰ Menu
      </button>

      <nav class="navlinks" aria-label="Primary navigation">
        <a href="index">Home</a>
        <a href="services">Services</a>
        <a href="work">Past Work</a>
        <a href="consultation">Request a Consultation</a>
      </nav>

      <div id="mobileMenu" class="mobile-menu" aria-hidden="true">
        <nav class="mobile-links" aria-label="Mobile navigation">
          <a href="index">Home</a>
          <a href="services">Services</a>
          <a href="work">Past Work</a>
          <a href="consultation">Request a Consultation</a>
        </nav>
      </div>
    </div>
  </div>
</header>

<main id="main">

  <!-- HERO -->
  <section class="hero-banner" aria-label="Hero">
    <div class="container">
      <div class="hero-inner">

        <div class="hero-kicker">⚡ Built for contractors & service businesses</div>

        <h1>Professional websites that help you win more work.</h1>

        <p>
          Clean, fast, mobile-first sites designed to build trust and drive calls.
          No templates. No fluff. You own the code and the brand.
        </p>

        <div class="hero-actions">
          <a class="btn hero-primary" href="consultation">Start a Consultation</a>
          <a class="btn hero-secondary" href="services.html">View Packages</a>
        </div>

        <div class="hero-trust" aria-label="Trust highlights">
          <span>✅ Mobile-first</span>
          <span>✅ SEO-ready</span>
          <span>✅ You own it</span>
        </div>

      </div>
    </div>
  </section>

  <!-- SERVICES -->
  <section class="section services-glow">
    <div class="container">
      <h2>Built to look premium — and perform.</h2>
      <p class="sub">Everything you need to launch with confidence and grow from there.</p>

      <div class="grid">
        <div class="card">
          <div class="icon screen">🖥️</div>
          <h3>Website Build</h3>
          <p>Modern multi-page or landing site built to convert visitors into calls and requests.</p>
        </div>

        <div class="card">
          <div class="icon orange">⚡</div>
          <h3>Logo & Brand</h3>
          <p>Clean logo system + colors and visuals that make your business look established.</p>
        </div>

        <div class="card">
          <div class="icon blue">◎</div>
          <h3>SEO & Speed</h3>
          <p>Structured pages, metadata, and performance polish so you show up and load fast.</p>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="footer">
  <div class="container">
    <div class="foot">
      <div class="small">
        <strong>Palmetto Digital</strong> • Websites & branding for service businesses
      </div>
      <div class="small">
        © <span id="year"></span> Palmetto Digital. All rights reserved.
      </div>
    </div>
  </div>
</footer>

<script>
  document.getElementById('year').textContent = new Date().getFullYear();
</script>
<script src="main.js"></script>

</body>
</html>
